"""Empty on purpose: its presence here is what puts the project root on
sys.path during test collection, so `from core import ...` resolves the
same way whether tests run via `pytest` or `python -m pytest`.
"""
